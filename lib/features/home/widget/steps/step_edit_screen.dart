import 'package:flutter/material.dart';

class StepEditScreen extends StatelessWidget {
  const StepEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Sleep"),
      ),
    );
  }
}
